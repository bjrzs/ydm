import { t } from '../old_i18n'

export function getKeyboardShortcuts () {
  return {
    'file.new': {
      label: t('shortcuts.file.new'),
      accelerator: 'CmdOrCtrl+N'
    }
    // ... existing code ...
  }
}
